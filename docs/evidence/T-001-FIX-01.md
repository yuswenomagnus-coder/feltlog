# Evidence Receipt: T-001-FIX-01

Commit: SELF (T-001-FIX-01 response commit; resolve with `git rev-parse --short HEAD`)
Deployment ID: n/a
Environment: local Next.js dev server at `http://127.0.0.1:3000`; local command checks
Producer: Implementer (Codex)
Reviewer: pending fresh Reviewer session
Date: 2026-07-05

## Acceptance Criteria Evidence

- SHOULD-FIX-001: Today's Log display is filtered to entries whose timestamp falls on the current local calendar date used by the page render: [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:14), [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:16), [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:150).
- SHOULD-FIX-001: Today's Log count and list render from the filtered `logEntries` array: [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:103), [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:104), [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:107), [app/page.tsx](/Users/ybs-m5/Downloads/feltlog/app/page.tsx:109).
- SHOULD-FIX-001: The write helper still appends to the full local JSON array; `git diff -- lib/day-log.ts` produced no output, and the append path remains unchanged: [lib/day-log.ts](/Users/ybs-m5/Downloads/feltlog/lib/day-log.ts:59).
- SHOULD-FIX-002: Hosted-persistence risk is now explicit in the original T-001 evidence receipt: [docs/evidence/T-001-EVIDENCE.md](/Users/ybs-m5/Downloads/feltlog/docs/evidence/T-001-EVIDENCE.md:230).
- SHOULD-FIX-002: Hosted-persistence risk is now explicit in `CURRENT_STATE.md` open risks: [docs/status/CURRENT_STATE.md](/Users/ybs-m5/Downloads/feltlog/docs/status/CURRENT_STATE.md:28).
- NIT-001: Rollback line now names reverting T-001 implementation commit `abcbddd` in full rather than deleting a partial file list: [docs/design/IMPLEMENTATION-PLAN-T-001.md](/Users/ybs-m5/Downloads/feltlog/docs/design/IMPLEMENTATION-PLAN-T-001.md:26).
- `CURRENT_STATE.md` points at this fix receipt and the next fresh Reviewer action: [docs/status/CURRENT_STATE.md](/Users/ybs-m5/Downloads/feltlog/docs/status/CURRENT_STATE.md:3), [docs/status/CURRENT_STATE.md](/Users/ybs-m5/Downloads/feltlog/docs/status/CURRENT_STATE.md:18).

## Changed Files

- `app/page.tsx`
- `docs/design/IMPLEMENTATION-PLAN-T-001.md`
- `docs/evidence/T-001-EVIDENCE.md`
- `docs/evidence/T-001-FIX-01.md`
- `docs/status/CURRENT_STATE.md`

Local evidence fixture only, not committed: `data/day-log.json` is gitignored and was used to seed yesterday/current-day entries for the manual verification.

## Commands run / Results / Logs & URLs

1. `pwd`
   - Result: `/Users/ybs-m5/Downloads/feltlog`

2. `git status --short --branch`
   - Initial result before edits:
     ```
     ## master...origin/master
     ```

3. `date '+%Y-%m-%d %H:%M:%S %Z %z'`
   - Result:
     ```
     2026-07-05 00:25:13 CEST +0200
     ```

4. Manual old-entry seed in gitignored `data/day-log.json`
   - Added entry:
     ```
     {
       "worker": "Worker B",
       "task": "T-001-FIX-01 yesterday guard 2026-07-04",
       "status": "Blocked",
       "timestamp": "2026-07-04T08:00:00.000+02:00"
     }
     ```

5. Baseline before production-code edit:
   - Command:
     ```
     node -e "const url='http://127.0.0.1:3000'; const old='T-001-FIX-01 yesterday guard 2026-07-04'; fetch(url).then(r=>r.text()).then(t=>console.log(JSON.stringify({url, containsYesterdayGuard:t.includes(old), savedEntriesText:(t.match(/>([0-9]+)<!-- --> saved <!-- -->(entry|entries)</)||[])[1] || null})))"
     ```
   - Result:
     ```
     {"url":"http://127.0.0.1:3000","containsYesterdayGuard":true,"savedEntriesText":"3"}
     ```

6. After date-scope display fix, old entry hidden from Today's Log but still on disk:
   - Page check result:
     ```
     {"url":"http://127.0.0.1:3000","containsYesterdayGuard":false,"savedEntriesText":"0","emptyState":true}
     ```
   - Disk check:
     ```
         "task": "T-001-FIX-01 yesterday guard 2026-07-04",
     ```

7. Fresh save from the current local calendar date still appears:
   - Command submitted the same form/server-action path by scraping the `$ACTION_ID_*` hidden field and POSTing `worker`, `task`, and `status`.
   - Result:
     ```
     {
       "url": "http://127.0.0.1:3000",
       "task": "T-001-FIX-01 fresh today save local 2026-07-05",
       "postStatus": 200,
       "pageContainsFreshTask": true,
       "jsonContainsFreshTask": true,
       "savedEntry": {
         "worker": "Worker B",
         "task": "T-001-FIX-01 fresh today save local 2026-07-05",
         "status": "Done",
         "timestamp": "2026-07-04T22:29:49.750Z"
       }
     }
     ```
   - Final page check after two current-local-date saves during verification:
     ```
     {"url":"http://127.0.0.1:3000","containsYesterdayGuard":false,"containsFreshTodaySave":true,"savedEntriesText":"2"}
     ```

8. `git diff -- lib/day-log.ts`
   - Result: no output.

9. `bash scripts/check-structure.sh`
   - Result:
     ```
     STRUCTURE CHECK: PASSED
     ```
   - Exit code: `0`

10. `npm run lint`
    - Result:
      ```
      > feltlog@1.0.0 lint
      > eslint .
      ```
    - Exit code: `0`

11. `npm run build`
    - Result:
      ```
      > feltlog@1.0.0 build
      > next build

      ▲ Next.js 16.2.10 (Turbopack)

        Creating an optimized production build ...
      ✓ Compiled successfully in 876ms
        Running TypeScript ...
        Finished TypeScript in 1006ms ...
        Collecting page data using 4 workers ...
        Generating static pages using 4 workers (0/2) ...
      ✓ Generating static pages using 4 workers (2/2) in 177ms
        Finalizing page optimization ...

      Route (app)
      ┌ ƒ /
      └ ○ /_not-found


      ○  (Static)   prerendered as static content
      ƒ  (Dynamic)  server-rendered on demand
      ```
    - Exit code: `0`

## What would prove this wrong?

- A fresh Reviewer seeds an entry with a prior local calendar date and sees it rendered under Today's Log.
- A fresh Reviewer saves a current-local-date entry and does not see it rendered under Today's Log.
- `data/day-log.json` loses prior entries after a current-day save.
- `bash scripts/check-structure.sh`, `npm run lint`, or `npm run build` fails on the T-001-FIX-01 response commit.
- A fresh Reviewer finds touched files outside the three requested findings.

## What was checked / not checked

Checked:

- Old local-date entry remains in `data/day-log.json` but is absent from the rendered Today's Log.
- Fresh current-local-date save is appended to `data/day-log.json` and appears in the rendered Today's Log.
- `lib/day-log.ts` has no diff, so the write path was not filtered.
- Required local checks: structure, lint, build.
- Original evidence receipt and current state both carry the hosted-persistence risk note.
- Rollback instruction names reverting T-001 implementation commit `abcbddd` in full.

Not checked:

- Hosted preview or production deployment; T-001 remains local-only.
- Independent Reviewer or Verifier reproduction; that is the next action.
- Browser click automation; the fresh save used the real server-action form POST path from the rendered page.

## Risks remaining

- Local filesystem persistence uses `fs/promises` to write `process.cwd()/data/day-log.json`; this will NOT survive hosted deployment unchanged because most hosting platforms expose read-only or ephemeral filesystems between requests/deploys. Before any T-002+ task that requires hosted persistence, redesign persistence to a real hosted store or equivalent durable backing service.
- `npm audit --omit=dev` previously reported two moderate PostCSS findings through Next `16.2.10`; npm's proposed force fix would install `next@9.3.3`, a breaking downgrade outside this approved slice.
- T-001-FIX-01 still requires fresh Reviewer re-check.

## Verdict: CANDIDATE_READY

Next action: fresh Reviewer re-check (fresh session).
